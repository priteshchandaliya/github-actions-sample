Here's a step-by-step guide on how to create a GitHub Actions project

This will create a workflow:

- Provision a EC2 instance 
- Install Docker
- Run a nginx container
- Allow SSH access to the VM via a password
